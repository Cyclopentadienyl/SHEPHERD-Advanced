# Deployment configs (v3)

Pick one:
- `windows.yaml` — Windows 11 x86 (Blackwell)
- `linux_x86.yaml` — Linux x86 workstation (Blackwell)
- `linux_arm_dgx.yaml` — NVIDIA DGX Spark (aarch64, Blackwell)

Use with scripts in `scripts/deploy/`. No `/env` folder is needed.
