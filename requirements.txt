# SHEPHERD-Advanced dependencies are managed via:
#   - pyproject.toml          : all pure-Python deps (single source of truth)
#   - deploy.sh / deploy.cmd  : CUDA-specific torch/torchvision/torchaudio + PyG
#
# Do NOT install via this file. Run instead:
#   Linux:    ./deploy.sh
#   Windows:  deploy.cmd
