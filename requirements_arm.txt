# Linux ARM (DGX Spark, aarch64) — prefer SDPA + hnswlib
# PyTorch 2.10 required for pyg-lib compatibility with cu130 (CUDA 13.0)
torch>=2.10
torchvision>=0.20
torchaudio>=2.10
xformers>=0.0.28
numpy>=1.26
scipy>=1.11
pyyaml>=6.0
pydantic>=2.5
tqdm>=4.66
hnswlib>=0.8
# faiss-cpu/faiss-gpu on ARM are optional/experimental; add only after validation
