# Linux ARM (DGX Spark, aarch64) — prefer cuDNN SDPA + hnswlib
# PyTorch 2.9.0 + cu130 unified across all platforms for ecosystem stability
# NOTE: Use exact torch==2.9.0 to avoid 2.9.1 which breaks pyg-lib compatibility
torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0

# Core scientific computing
# NumPy 2.3+ requires Python 3.11+, so use 2.2.x for Python 3.10 compatibility
numpy>=2.0,<2.3
scipy>=1.14

# Configuration and utilities
pyyaml>=6.0
pydantic>=2.7
tqdm>=4.66

# Vector retrieval (hnswlib for ARM compatibility)
hnswlib>=0.8

# NOTE: On ARM/DGX Spark, use cuDNN 9.x SDPA (built into PyTorch 2.9+)
# FlashAttention-2 is NOT compatible with ARM architecture
# xformers is optional: python scripts/launch/shep_launch.py --install-accelerators
