# Windows x86 (Blackwell) — baseline; pin exact versions after local verification
# PyTorch 2.9.0 + cu130 unified across all platforms for ecosystem stability
# NOTE: Use exact torch==2.9.0 to avoid 2.9.1 which breaks pyg-lib compatibility
torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0

# Core scientific computing
# NumPy 2.3+ requires Python 3.11+, so use 2.2.x for Python 3.10 compatibility
numpy>=2.0,<2.3
scipy>=1.14

# Configuration and utilities
pyyaml>=6.0
pydantic>=2.7
tqdm>=4.66

# Vector retrieval (hnswlib preferred over FAISS for cross-platform)
hnswlib>=0.8

# NOTE: Acceleration libraries (flash-attn, xformers, sageattention) are OPTIONAL
# Install via: python scripts/launch/shep_launch.py --install-accelerators
# This avoids compatibility issues with third-party libs that may lag behind PyTorch releases
