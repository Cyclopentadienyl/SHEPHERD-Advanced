"""
Deployment-config validator — RESERVED home (not yet implemented).
==================================================================
Reserved home for validating the committed deployment configuration
(``configs/deployment.yaml``) against its JSON schema
(``configs/schemas/deployment.schema.json``).

Status: intentionally empty until deployment-config validation is built.
Nothing on any runtime path imports this module yet. See the config-authority
decision record (``docs/CONFIG_AUTHORITY.md``) for why this stays a documented
reserved home rather than being deleted.

Module: src/config/config_validator.py
"""
