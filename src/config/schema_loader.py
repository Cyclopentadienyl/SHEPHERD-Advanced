"""
Config schema loader — RESERVED home (not yet implemented).
===========================================================
Reserved home for loading and parsing the JSON schema(s) under
``configs/schemas/`` (e.g. ``deployment.schema.json``) that
``config_validator`` will validate the committed deployment configuration against.

Status: intentionally empty until deployment-config validation is built.
Nothing on any runtime path imports this module yet. See the config-authority
decision record (``docs/CONFIG_AUTHORITY.md``).

Module: src/config/schema_loader.py
"""
